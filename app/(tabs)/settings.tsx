import { Text, View } from "react-native";

const Settings = () => {
  return (
    <View className="flex-1 bg-background p-5">
      <Text>Settings</Text>
    </View>
  );
};

export default Settings;
